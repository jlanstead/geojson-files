# ALPI_Map

Minimal MapLibre + TomTom Power BI visual.
Fields:
- Legend (text)
- Latitude (number)
- Longitude (number)

## Build
npm install
npx -y powerbi-visuals-tools@6.1.3 --version
npm run package
